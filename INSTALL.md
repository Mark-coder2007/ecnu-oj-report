# 公开仓库说明

这个仓库是 **showcase / docs only** 版本，不是完整可运行版本。

## 这里有什么

- 项目介绍
- 公开可看的工作流说明
- 示例配置结构
- 示例目录布局

## 这里没有什么

- 不包含 `scripts/` 自动化实现
- 不包含 `package.json` / `requirements.txt` 这类运行依赖声明
- 不包含真实 `config.json`
- 不包含登录态、导出代码、截图、报告产物

## 为什么这样拆

仓库被拆成两个用途不同的版本：

1. 公开仓库：用于展示项目思路、目录结构、配置形状和文档。
2. 私有仓库：保存完整实现、脚本与可执行 skill 内容。

这样做的目标是让别人可以看懂项目大概做什么，但拿不到直接可运行的完整自动化实现。

## 如果你是来看公开说明的

建议按这个顺序阅读：

1. [README.md](README.md)
2. [WORKFLOW.md](WORKFLOW.md)
3. [examples](examples/)

## 如果你是仓库拥有者

完整实现应维护在单独的私有仓库中，并且至少满足这些要求：

- `config.json`、`ecnu-auth.json`、`out/`、`data/` 不进版本库
- 自动化边界和学术合规声明写清楚
- 模板处理逻辑不能静默清空正文
- 缺代码 / 缺截图时要有完整性检查

## 合规提醒

如果私有版包含“自动解题并提交”的能力，它只能用于：

- 本人账号
- 非考试 / 非竞赛中的允许场景
- 课程、教师和 OJ 规则允许 AI 辅助的前提下
